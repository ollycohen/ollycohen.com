.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Secrets — never commit
.env
.env.local
.env.*.local

# Claude Code config — track shared hooks + settings, ignore per-user state
.claude/*
!.claude/hooks/
!.claude/settings.json

# Old Webflow CSV exports (kept locally for reference)
webflowExport/

# High-res photo originals — uploaded to Cloudinary, not tracked in git.
# Logos under images/**/logo/ stay tracked since they're small and version-stable.
images/**/cover/
images/**/gallery/
images/**/originals/
